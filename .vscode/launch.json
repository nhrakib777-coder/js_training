{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run JS (VS Code Only)",
      "program": "${file}",
      "console": "internalConsole"
    }
  ]
}
